#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @g: integer to check
 *
 * Return: int _abs(int)
 */
int _abs(int g)
{
	if (g >= 0)
	{
		return (g);
	}
	return (-g);
}
